// Destructuring : take in object or array and convert into smaller objects / variables / elements

const alphabet = ['A', 'B', 'C', 'D', 'E', 'F'];

const numbers = ['1', '2', '3', '4', '5', '6'];

// Normal way of accesing every element

const a = alphabet[0];
const b = alphabet[1];

console.log(a); // A

console.log(b); // B




