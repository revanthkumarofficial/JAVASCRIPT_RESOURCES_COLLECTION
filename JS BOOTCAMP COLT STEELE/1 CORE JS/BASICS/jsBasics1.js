// JavaScript including variables, function names, class names, and operators are case-sensitive

// this is a single-line comment

/*
 * This is a block comment that can
 * span multiple lines
 */

// STATEMENTS
// it is recommended to always use the semicolon to end a statement.

var a = 10;
var b = 20;

// Use curly bases to combine multiple statements as follows:

if (a > b) {
  console.log("a is greater than b");
  return 1;
}

// An expression is a piece of code that evaluates to a value

2 + 1; // 3

a + b;

/** JS KEYWORDS
* 
abstract arguments	await	boolean
break	byte	case	catch
char	class	const	continue
debugger	default	delete	do
double	else	enum	eval
export	extends	false	final
finally	float	for	function
goto	if	implements	import
in	instanceof	int	interface
let	long	native	new
null	package	private	protected
public	return	short	static
super	switch	synchronized	this
throw	throws	transient	true
try	typeof	var	void
volatile	while	with	yield
  * 
  */
